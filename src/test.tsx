import React from "react";
import ReactDOM from "react-dom";
import ReactWeekTimeRangePicker from './index'

ReactDOM.render(
  <ReactWeekTimeRangePicker hasHalfHour={true} />,
  document.getElementById("root")
);